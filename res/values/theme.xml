<?xml version="1.0" encoding="utf-8"?>
<resources>
  <!-- Colors -->
  <color name="black">#000000</color>
  <color name="orange">#fcaf3e</color>

  <!-- Application theme -->
  <style name="CastIO" parent="android:Theme.Holo.Light">
    <item name="android:windowBackground">@color/black</item>
    <item name="android:windowActionBar">true</item>
    <item name="android:windowActionBarOverlay">true</item>
    <item name="android:actionBarStyle">@style/ActionBar</item>
    <item name="android:icon">@drawable/icon_black</item>
  </style>

  <!-- General styles for action bar -->
  <style name="ActionBar" parent="android:Widget.ActionBar">
    <item name="android:background">@color/orange</item>
    <item name="android:backgroundSplit">@color/orange</item>
    <item name="android:displayOptions">homeAsUp|showHome|showTitle</item>
  </style>

</resources>
